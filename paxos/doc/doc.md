In this step you should complete the rest of the protocol. If you
built a good simulator structure in the previous step, you should be
able to focus on the details of Paxos itself and it should be
straightforward to add each new case.

You should implement this using an iterative process:

*   Run "make" to see how far you get

*   Look at the expected output and re-examine the protocol to make
    sure you understand why the expected output is what it is and
    what rules lead to it

*   Implement just enough code to correctly generate the next line
    of output. Do not try to do too much at once—focus on a quick
    development cycle

*   Repeat
